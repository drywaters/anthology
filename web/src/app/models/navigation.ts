export interface NavigationItem {
    id: string;
    label: string;
    icon?: string;
    route: string;
}

export interface ActionButton {
    id: string;
    label: string;
    icon?: string;
    route?: string;
}
